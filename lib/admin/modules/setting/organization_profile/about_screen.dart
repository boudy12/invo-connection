import 'package:flutter/material.dart';

class OrganizationAboutScreen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        Text(
          'About'
        ),
        SizedBox(
          height: 15,
        ),
        Text(
          'About'
        ),
      ],
    );
  }
}
