import 'package:flutter/material.dart';

class OrganizationProjectScreen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        Text(
          'Project'
        ),
        SizedBox(
          height: 15,
        ),
        Text(
          'Project'
        ),
      ],
    );
  }
}
