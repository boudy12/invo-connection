import 'package:flutter/material.dart';

class ConnectionAppAdminAllChatsScreen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'chat'
      ),
    );
  }
}
